# 폼체커(FormChecker) — 실시간 운동자세 카운터 앱 설계 문서

> 앱 이름: **폼체커** · 1차 프로젝트 (모바일 트랙) · Kotlin / Android Studio / Supabase / LiteRT(TensorFlow Lite)

---

## 1. 문제 정의

집에서 혼자 스쿼트를 할 때 두 가지가 안 된다.
1. **몇 개 했는지** 정확히 세기 귀찮다 (직접 세다 놓침).
2. **자세가 맞는지** 스스로 모른다 (무릎이 안으로 말리는지, 깊이가 충분한지).

카메라로 실시간 관절을 추적해 **자동 카운팅 + 자세 피드백**을 온디바이스로 제공하고, 네트워크가 없어도 운동은 100% 가능하게 만든다. Supabase는 "기록 저장/동기화"에만 관여하고 "운동 자체"에는 관여하지 않는 것이 설계의 핵심 원칙이다.

**스코프 컷 (중요)**: 5일 내 완성도를 위해 **스쿼트 1종만** 정식 지원한다. 푸시업은 시간이 남을 때만 추가하는 스트레치 목표로 둔다.

---

## 2. 전체 아키텍처

```
UI (Compose)
  ↓ observe
ViewModel (WorkoutViewModel, HistoryViewModel ...)
  ↓ call
UseCase (StartSessionUseCase, RecordRepUseCase, SyncPendingSessionsUseCase ...)
  ↓ call
Repository (WorkoutRepository) ─┬─ Local: Room (source of truth, 즉시 반영)
                                 └─ Remote: Supabase (Postgrest) — WorkManager로 비동기 동기화

CameraX ImageAnalysis ──frame──▶ LiteRT Interpreter(MoveNet) ──keypoints──▶ PoseAnalyzer(각도 계산 + 상태머신) ──RepEvent──▶ ViewModel
```

- **레이어드 + Repository 패턴**. Room이 항상 1차 저장소(오프라인 우선), Supabase는 동기화 대상일 뿐 앱 동작을 막지 않는다.
- DI는 Hilt 사용. ML 파이프라인(카메라→추론→상태머신)은 UI 레이어와 완전히 분리된 별도 모듈(`:pose-engine`)로 둬서, 나중에 콜드스타트/프레임드랍 벤치마크를 UI 없이도 돌릴 수 있게 한다 (재현성 확보의 핵심 장치).

---

## 3. AI 파이프라인

### 3.1 모델
- **MoveNet Lightning** (single-pose, 17 keypoints), LiteRT 포맷. Thunder(고정확도)는 예비 옵션 — CPU 기준 실시간성이 안 나오면 Lightning으로 확정.
- 의존성: `com.google.ai.edge.litert` (신규 프로젝트 권장 경로). GPU delegate를 옵션으로 붙여 **"성능 개선 전후"** 비교 스토리를 만든다 (baseline: CPU delegate → optimized: GPU delegate 또는 스레드 수 튜닝).

### 3.2 추론 → 판정 로직
1. CameraX `ImageAnalysis`에서 프레임 수신 (해상도 다운스케일: 256x256 정도로 리사이즈 후 추론 — 콜드스타트/프레임드랍에 직접 영향).
2. 각 keypoint의 confidence < 0.3이면 해당 프레임은 "무효 프레임"으로 스킵 (저조도·가려짐 대응, 안정성 요구사항).
3. 힙-무릎-발목 각도 계산 → EMA(지수이동평균)로 스무딩 (프레임 튐으로 인한 오탐지 방지).
4. **상태머신**으로 rep 카운트:
   `STANDING(각도>160°) → DESCENDING → BOTTOM(각도<100°, 목표 깊이) → ASCENDING → STANDING` 사이클 완주 시 rep +1.
5. 자세 피드백: 무릎 x좌표가 발목보다 과도하게 안쪽이면 "무릎 정렬" 경고, BOTTOM 각도가 120° 이상이면 "깊이 부족" 경고.

### 3.3 재현성 확보 (핵심 설계 포인트)
라이브 카메라 데모는 사람마다·매번 다르므로 **재현 불가능**하다. 그래서:
- 정면/측면/저조도/빠른동작/다인 등장 등 **5개 시나리오 테스트 영상**을 사전 녹화하고, 사람이 직접 라벨링한 정답 rep 수를 기록해둔다.
- 벤치마크 시에는 라이브 카메라 대신 `MediaMetadataRetriever`로 영상 프레임을 뽑아 동일 파이프라인(`:pose-engine`)에 주입 → 매번 동일한 입력으로 동일한 결과가 나오는지 확인한다.
- 이 방식이 "다시 돌려도 결과가 일정하다"는 재현성 요구를 만족시키는 핵심 장치다.

### 3.4 자세 정확도 판정 — 4단계 하이브리드 파이프라인

> 3.2절의 규칙기반 판정을 1단계로 두고, 그 위에 통계 보정(2단계)·학습 분류기(3단계)·개인화(4단계)를 얹어 하이브리드로 확장한다. **지속적 피드백 재학습 루프(5단계)는 "AI-Hub 데이터로 1회만 학습"하는 전략을 채택함에 따라 스코프에서 명시적으로 제외**한다.

**1단계 — 규칙기반 (항상 실행, 기본 레이어)**
3.2절에서 정의한 각도 임계값 로직 그 자체. 3단계 모델이 로드에 실패하거나 신뢰도가 낮을 때의 fallback이자, "왜 틀렸는지"를 사용자에게 바로 설명할 수 있는 근거로 계속 유지된다.

**2단계 — 임계값 통계적 검증·보정 (모델 학습 아님, 1회성 데이터 분석)**
- 목적: 1단계 임계값(무릎각도 100°/160° 등)이 감으로 정한 숫자가 아니라 실측 데이터로 검증된 값인지 확인.
- 데이터: AI-Hub "크로스핏 동작 데이터"(dataSetSn=71422)의 **스쿼트 > 에어 스쿼트** 항목만 추출. 이 항목은 정상(40%)/발뒤꿈치 오류(20%)/엉덩이 하방 오류(20%)/고관절 오류(20%) 4가지 상태로 라벨링돼 있고, `motion_category3`(에어 스쿼트) + `motion_category4`(정상/오류 세부)로 구분된 JSON 어노테이션 + 2D·3D keypoint CSV가 함께 제공됨. 8채널 멀티뷰 촬영이라 다양한 카메라 각도 검증도 가능.
- ⚠ **keypoint 포맷 차이 주의**: 이 데이터셋은 COCO 17이 아니라 **25개 keypoint**(OpenPose BODY_25 계열 — 발가락·발꿈치 세부 포인트 포함)로 라벨링돼 있다. MoveNet은 17개 keypoint만 출력하므로, 이 25개 중 MoveNet과 이름이 겹치는 지점(Shoulder/Elbow/Wrist/Hip/Knee/Ankle 등)만 추출해서 매핑하고, MoveNet에 없는 발가락·발꿈치 포인트는 이번 분석에서 제외한다.
- ⚠ **오류 라벨 매핑 확인 필요**: "발뒤꿈치 오류/엉덩이 하방 오류/고관절 오류"가 1단계에서 정의한 `knee_valgus`/`shallow_depth`와 정확히 1:1 대응하지 않을 수 있다. 데이터 설명서(구축활용가이드)를 다운로드해 각 오류의 정확한 판정 기준을 먼저 확인하고, 필요하면 1단계 `error_flags`의 종류 자체를 이 데이터의 오류 분류에 맞춰 재정의한다 (예: `heel_lift` 플래그 추가).
- 방법: 라벨별(정상/오류1/오류2/오류3) keypoint 각도 분포를 pandas/numpy로 통계 분석. `annotation.json`의 `actor` 필드(키, 성별, 나이, 숙련도)로 체형별 군집화도 가능.
- 산출물: 1단계 코드의 임계값 상수를 데이터 기반으로 보정한 값, 오류 플래그 종류 재정의(필요 시).
- 사전 확인 필요: 안심존/IRB 승인 절차가 이 데이터셋(스포츠 분야)에도 실제로 적용되는지 문의처(pure@elancer.co.kr, 이랜서)로 먼저 확인 — 페이지에 공통 안내 문구가 보이지만 헬스케어 데이터가 아니라 적용 여부가 불확실하다.

**3단계 — 학습 기반 분류기 (실제 모델 학습, AI-Hub 데이터로 1회 학습 후 고정)**
- 목적: 1단계 규칙이 못 잡는 복합오류(엉덩이 하방+고관절 오류 동시 발생 등)·템포(하강 속도) 문제를 통계적으로 보완.
- 입력 피처: raw keypoint 좌표가 아니라, 이를 가공한 관절 각도·각속도 시퀀스 (MoveNet과 겹치는 keypoint만 사용).
- 모델: AI-Hub 참조모델(PoseC3D — skeleton을 히트맵 이미지로 재구성해 처리하는 CNN, "오류 분류" 태스크에서 Top-3 정확도 99.88% 기록)을 온디바이스에 그대로 쓰지 않고, keypoint 시퀀스만 입력받는 경량 1D-CNN/LSTM을 별도로 학습해 `.tflite`로 변환 후 `:pose-engine`에 MoveNet과 함께 번들. PoseC3D의 높은 정확도는 "이 라벨로 오류를 구분하는 게 통계적으로 가능하다"는 선례로 참고.
- 검증: **cross-subject split** (동일 `actor` 데이터가 train/test에 동시에 들어가지 않도록 분리) — 체형을 외워서 생기는 가짜 고정확도 방지.
- 운영 방식: 1단계와 3단계를 항상 같이 실행 → 결과가 불일치하면 1단계(설명 가능한 쪽)를 우선 적용하고, 불일치 케이스를 로그로 기록 (재학습에는 쓰지 않지만 회고·사후분석 자료로 보관).
- 재학습 정책: **AI-Hub 데이터로 1회만 학습 후 고정**. 데이터셋에 없는 패턴(예: 이 데이터의 3가지 오류 유형 외의 자세 문제)은 이번 스코프에서 못 잡는다는 한계를 회고에 명시한다 (5단계를 제외한 것에 대한 직접적 트레이드오프).

**4단계 — 개인별 캘리브레이션 (런타임 개인화, 모델 학습과 무관)**
- 목적: 1·3단계의 "인구 전체 기준" 판정을 사용자 개인 기준으로 미세조정.
- 방법: 세션 초반(또는 별도 캘리브레이션 모드) N회 동안 사용자의 실제 최대/최소 관절각도를 기록해 개인 기준선(baseline) 산출. (참고: 3단계 학습 데이터의 `actor_height` 등 체형 메타데이터로 "이 사용자와 비슷한 체형 그룹"의 기준을 초기값으로 참고할 수도 있음 — 필수는 아니고 여유가 있으면 고려)
- 판정 방식: 이후 rep은 인구 기준(1·3단계)과 개인 기준을 함께 평가하고, 개인 기준을 우선 적용.
- 저장: Room 로컬 저장 + 기존 오프라인 동기화 전략(7장) 그대로 재사용 — 새 인프라 불필요.
- 투자 대비 효과: 구현이 단순 통계(이동평균/최소·최대값)라 비용은 낮고 체감 정확도 개선 효과는 크다.

**파이프라인 흐름**
```
1단계(규칙, 항상 실행) ──┐
                        ├─→ 둘 다 실행, 불일치 시 1단계 우선 + 로그
3단계(모델, 항상 실행) ──┘
        ↑
2단계(1회성 통계 분석)이 1단계 파라미터를 보정

4단계(개인화)는 1·3단계 결과 위에 개인 기준선으로 한 번 더 보정
```

**명시적으로 제외한 항목 — 5단계 (지속적 피드백 재학습 루프)**
AI-Hub 데이터로 1회만 학습하는 전략을 채택했으므로, 사용자 피드백을 모아 서버에서 주기적으로 재학습하는 파이프라인(사람 검수 → 배치 재학습 → 검증 후 배포)은 스코프에서 제외한다. 트레이드오프는 명확하다 — 데이터셋에 없는 패턴(이 데이터셋이 못 담은 특정 체형·오류 유형)은 이후에도 개선되지 않는다. 이는 실제 프로덕트라면 리스크지만, 이번 프로젝트에서는 "리소스 대비 실익이 낮다고 판단해 제외했다"는 근거 있는 스코프 컷으로 회고에 남긴다.

---

## 4. Supabase DB 스키마

```sql
-- 운동 세션
create table workout_sessions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) not null,
  exercise_type text not null default 'squat' check (exercise_type in ('squat', 'pushup')),
  device_model text,
  os_version text,
  started_at timestamptz not null default now(),
  ended_at timestamptz
);

-- 세트
create table workout_sets (
  id uuid primary key default gen_random_uuid(),
  session_id uuid references workout_sessions(id) on delete cascade,
  set_number int not null,
  target_reps int,
  completed_reps int default 0
);

-- 개별 rep 기록
create table rep_records (
  id uuid primary key default gen_random_uuid(),   -- 클라이언트에서 생성 (오프라인 큐 idempotent upsert용)
  set_id uuid references workout_sets(id) on delete cascade,
  rep_number int not null,
  recorded_at timestamptz not null default now(),
  depth_score numeric,        -- 0~1, 목표각도 대비 깊이 비율
  form_score numeric,         -- 0~1
  is_valid boolean default true,
  error_flags jsonb           -- {"knee_valgus": true, "shallow_depth": false}
);

-- 성능 리포트 (기기별/세션별로 쌓아 리포트에 바로 활용)
create table performance_metrics (
  id uuid primary key default gen_random_uuid(),
  session_id uuid references workout_sessions(id) on delete cascade,
  cold_start_ms int,
  avg_inference_ms numeric,
  p95_inference_ms numeric,
  dropped_frame_ratio numeric,
  avg_fps numeric,
  delegate_type text,          -- 'cpu' | 'gpu'
  created_at timestamptz default now()
);
```

- RLS(Row Level Security): 모든 테이블 `user_id`(또는 조인 경로) 기준으로 본인 데이터만 read/write 가능하도록 정책 설정.
- `rep_records.id`를 클라이언트 UUID로 미리 생성해두면, 오프라인 큐에서 재전송해도 **upsert로 안전하게 멱등 처리**된다 (동기화 재시도 시 중복 삽입 방지).
- Room(로컬)에는 동일 스키마 + `sync_status`(pending/synced/failed) 컬럼만 추가로 둔다.

---

## 5. 화면 구조

```
Splash (모델 프리로드 비동기 시작 + 세션 체크)
  └─ Home (오늘의 목표 세트/횟수 설정 + 최근 기록 요약)
       ├─ Workout (카메라 풀스크린 + 스켈레톤 오버레이 + 카운터 HUD + 실시간 피드백 배너)
       │     └─ SessionSummary (세트별 rep 리스트, 폼 점수, 성능 지표 카드, 저장 상태)
       ├─ History (세션 리스트, 동기화 대기(pending) 배지)
       └─ Settings (delegate 선택 CPU/GPU, 해상도, 강제 동기화, 로그아웃)
```

- **Workout 화면**이 핵심. 카메라 프리뷰 위에 Canvas로 스켈레톤을 그리고, 상단에 rep count/목표, 하단에 피드백 배너("무릎을 벌려주세요" 등)를 얹는다.
- **SessionSummary**에 콜드스타트/추론지연/프레임드랍 수치를 바로 카드로 노출해서, 데모 영상 찍을 때 "이 화면 하나로 성능 스토리 설명 가능"하게 만든다.

---

## 6. 상태·에러 처리 구조 (안정성)

```kotlin
sealed class WorkoutUiState {
    object Idle : WorkoutUiState()
    object ModelLoading : WorkoutUiState()
    data class Tracking(val reps: Int, val feedback: String?) : WorkoutUiState()
    object Paused : WorkoutUiState()
    data class Error(val type: WorkoutError) : WorkoutUiState()
}

sealed class WorkoutError {
    object CameraPermissionDenied : WorkoutError()
    object CameraUnavailable : WorkoutError()
    object ModelLoadFailed : WorkoutError()   // Lightning 로드 실패 → 재시도 1회 → 그래도 실패면 안내 후 종료
}
```

- **네트워크 실패는 하드 에러가 아니다** — UI 상단에 "오프라인, N건 대기 중" 배지만 표시하고 운동 흐름을 절대 막지 않는다.
- 저신뢰(confidence 낮음) 프레임은 에러가 아니라 그냥 스킵 + "카메라 각도를 조정해주세요" 안내로 처리 (이상 입력 방어).
- `interpreter.run()`은 항상 try-catch로 감싸고, 실패한 프레임은 드롭 카운트에 반영 후 다음 프레임으로 진행 (한 프레임 실패로 세션 전체가 죽지 않게).

---

## 7. 오프라인 동기화 전략

1. 모든 기록은 **Room에 먼저, 무조건** 저장한다 (Supabase 연결 여부와 무관).
2. `sync_status = pending`인 로우를 WorkManager `CoroutineWorker`(주기적 + 즉시 트리거)가 배치로 Supabase에 upsert.
3. 실패 시 지수 백오프 재시도, 최대 재시도 초과분은 `failed`로 표시하고 Settings에서 수동 "강제 동기화" 버튼 제공.
4. 클라이언트 UUID 기반 upsert라 재시도로 인한 중복이 없음 (충돌 정책 별도 불필요, append-only 데이터라 단순함).

**검증 방법**: 비행기모드 on/off를 인위적으로 토글하며 세션 5~10개를 생성 → 재연결 후 동기화 성공률과 지연시간을 표로 기록.

---

## 8. 측정 방법론 (수치 지표)

| 지표 | 측정 도구 | 방법 |
|---|---|---|
| **rep 카운팅 정확도** (핵심 지표) | 5개 라벨링된 테스트 영상 | 정답 rep 수 대비 앱 카운트 오차율. 스무딩 적용 전/후 비교표로 "개선 증명" |
| 콜드스타트 시간 | Macrobenchmark (`StartupTimingMetric`) | 10회 반복, median/p90 보고. 모델 로딩을 메인스레드 블로킹 → 백그라운드 비동기로 바꾼 전/후 비교 |
| 프레임 드롭 / 추론 지연 | JankStats + `System.nanoTime()` 프레임별 타임스탬프 | avg/p95 추론 시간(ms), UI 프레임 중 16.6ms(60Hz) 초과 비율 = dropped_frame_ratio. CPU delegate vs GPU delegate 전/후 비교 |
| 오프라인 동기화 성공률 | 인위적 airplane mode 토글 테스트 | N회 실패 유도 후 재연결 시 성공 건수/전체 건수 |

이 4개 수치를 `performance_metrics` 테이블에 세션마다 자동 적재해서, 최종 리포트는 쿼리 한 번으로 뽑을 수 있게 한다.

---

## 9. 개발 플로우 (단계·게이트 기반)

> 날짜 대신 **선행조건 → 작업 → 완료기준(exit criteria)**으로 흐름을 정의한다. 각 Phase는 이전 Phase의 완료기준을 충족해야 다음으로 넘어간다. Phase 0의 두 트랙(A/B)은 반드시 동시에 시작한다 — AI-Hub 승인 대기 시간이 얼마나 걸릴지 모르기 때문에, MVP 개발이 끝난 뒤에 신청하면 그만큼 전체 일정이 그대로 늘어진다.

```
Phase 0 (병렬 시작)
 ├─ Track A: 프로젝트 셋업
 └─ Track B: AI-Hub 데이터 신청 (승인 대기 시작)
        │
        ▼
Phase 1: MVP — 규칙기반 스쿼트 카운터 (1단계만)
        │  ── 완료기준 충족 시 "1차 제출 가능" 상태 확보
        ▼
Phase 2: 게이트 — AI-Hub 데이터 승인됐는가?
        │
   ┌────┴────┐
  아니오      예
   │          │
   ▼          ▼
Phase 6      Phase 3: 통계 보정 (2단계)
(1단계          │
 기준으로        ▼
 마무리)      Phase 4: 분류기 학습·통합 (3단계)
                │
                ▼
             Phase 5: 개인화 캘리브레이션 통합 (4단계)
                │
                ▼
             Phase 6: 최종 통합 성능측정 & 문서화
```

### Phase 0 — 사전 준비 (Track A/B 병렬)

**Track A: 프로젝트 셋업**
- Compose Navigation 뼈대, 카메라 권한 처리
- Supabase 프로젝트 생성 + 4장 DDL 적용 + RLS 정책, Auth(익명 로그인)
- Room 스키마, Hilt DI
- 완료기준: 빈 화면들이 서로 이동 가능한 빌드본이 나온다

**Track B: AI-Hub 데이터 신청**
- dataSetSn=71422 "크로스핏 동작 데이터" 신청, 신청목적 작성("개인 연구 및 공부" 등)
- 스쿼트 > 에어 스쿼트 항목의 라벨 구성(정상/발뒤꿈치 오류/엉덩이 하방 오류/고관절 오류) 및 keypoint 포맷(25개, MoveNet 17개와 다름) 사전 확인 — 데이터 설명서·구축활용가이드 다운로드
- 안심존/IRB 승인 필요 여부 확인 (문의처: pure@elancer.co.kr, 이랜서) — 스포츠 분야 데이터라 헬스케어 데이터와 달리 안심존이 불필요할 가능성도 있으니 우선 문의로 확정
- 완료기준: 승인 완료(또는 승인 불필요 확인) — 이 결과가 Phase 2 게이트를 결정한다

### Phase 1 — MVP: 규칙기반 스쿼트 카운터 (1단계만)

- LiteRT 의존성 + MoveNet Lightning 통합, CameraX `ImageAnalysis` 연동, 키포인트 Canvas 오버레이
- 관절 각도 계산, EMA 스무딩, rep 카운팅 상태머신(3.2절), 자세 피드백 로직
- Repository 동기화(Room↔Supabase), WorkManager 백그라운드 sync, 에러/오프라인 UI 반영
- SessionSummary/History 화면
- 완료기준:
  - 카메라 화면에 실시간 스켈레톤 표시
  - 스쿼트 1세트가 정확히 카운트됨
  - 비행기모드 켜고 운동해도 앱이 죽지 않고, 재연결 시 자동 동기화됨
  - 5개 테스트 영상(3.3절)으로 rep 카운팅 정확도 측정 가능

> **이 지점에서 앱은 이미 완결된 결과물이다.** Phase 3~5는 Phase 2 게이트 결과에 따라 진행 여부가 결정되는 확장 구간이다.

### Phase 2 — 게이트: AI-Hub 데이터 승인 확인

- 승인 완료 → Phase 3으로 진행
- 미승인/반려/과도한 대기 → Phase 6(문서화)으로 바로 이동, 2~4단계는 "설계문서상 확장 계획(3.4절)"으로만 남기고 실제 구현은 하지 않음. 회고에 "왜 여기서 멈췄는지"를 명시 (5단계를 제외한 논리와 동일한 맥락)

### Phase 3 — 통계 보정 (2단계)

- dataSetSn=71422에서 스쿼트 > 에어 스쿼트 항목만 추출, 25개 keypoint 중 MoveNet 17개와 겹치는 지점만 매핑
- 라벨별(정상/발뒤꿈치 오류/엉덩이 하방 오류/고관절 오류) keypoint 각도 분포를 pandas/numpy로 분석
- `annotation.json`의 `actor`(키/성별/나이/숙련도) 메타데이터로 체형별 군집화해 군집별 임계값 도출 가능
- Phase 1의 임계값 상수를 실측 분포 기준으로 보정, 필요 시 `error_flags` 종류를 이 데이터의 오류 분류에 맞춰 재정의
- 완료기준: 보정 전/후 임계값 비교표 확보, 코드에 보정값 반영 완료

### Phase 4 — 분류기 학습·통합 (3단계)

- 피처 엔지니어링: 매핑된 keypoint 좌표 → 관절 각도·각속도 시퀀스로 변환
- Train/test를 **cross-subject split**으로 분리 (동일 `actor`가 양쪽에 겹치지 않게)
- 경량 1D-CNN/LSTM 학습 → `.tflite` 변환 → `:pose-engine`에 MoveNet과 함께 번들 (AI-Hub 참조모델 PoseC3D는 오류분류 Top-3 99.88%를 기록했으나 영상 전체를 보는 무거운 모델이라 온디바이스엔 그대로 쓰지 않고 참고 선례로만 활용)
- 1단계·3단계를 항상 같이 실행하도록 통합, 불일치 시 1단계 우선 + 로그 기록 로직 추가
- 완료기준: held-out 테스트셋 정확도 확보, 1·3단계 불일치 로그로 "3단계가 실제로 추가로 잡아낸 케이스"를 최소 몇 건 이상 제시 가능

### Phase 5 — 개인화 캘리브레이션 통합 (4단계)

- 캘리브레이션 모드 UI(최초 N회 동안 개인 기준선 측정)
- 개인 기준선 Room 저장 + 기존 오프라인 동기화 전략(7장) 재사용
- 판정 로직에 "인구 기준 vs 개인 기준" 동시 평가 추가
- 완료기준: 캘리브레이션 전/후로 동일 사용자의 판정 결과가 달라지는 사례를 시연 가능

### Phase 6 — 최종 통합 성능측정 & 문서화

- Macrobenchmark로 콜드스타트 재측정, JankStats로 프레임드랍 재측정 (Phase 2~5를 거쳤다면 모델이 1개→2개로 늘어난 만큼 지연 변화도 같이 측정)
- 5개 테스트 영상으로 최종 rep 카운팅 정확도 측정 → GPU delegate/스레드 튜닝 등 최적화 전/후 비교표
- `performance_metrics` 테이블 기준 성능 리포트 작성
- 설계문서 최종화, 회고 작성(12장 질문 기준), 데모 영상 녹화

**리스크 대응**: GPU delegate 최적화가 기기에서 잘 안 붙으면(호환성 이슈 흔함) 대체로 "프레임 리사이즈 해상도 낮추기 + 추론 스레드 수 튜닝"으로 전/후 비교 스토리를 확보한다.

---

## 10. 기술 선택 근거 (비교 대상 포함)

### 10.1 DB: Supabase(Postgres) vs Firebase(Firestore)

| 기준 | Supabase (Postgres) | Firebase (Firestore) |
|---|---|---|
| 데이터 구조 | sessions→sets→reps→metrics가 FK로 자연스럽게 표현되는 관계형 구조 | 계층형 subcollection으로 구현 가능하나 원래 비정규화(denormalize) 지향 |
| 성능 리포트 쿼리 | SQL JOIN/집계로 "주간 평균 폼 점수" 같은 리포트를 한 번에 작성 | 복잡한 JOIN·집계가 필요하면 Supabase가 권장되는 편 |
| 보안(RLS) | RLS 정책이 DB 레벨에서 강제되어, 앱·크론잡·직접 SQL 연결 모두 동일한 보안 체크를 통과 | 보안 규칙은 SDK 접근만 보호, Admin SDK로는 우회 가능 |
| 오프라인 지원 | 기본 제공 없음 → Room + WorkManager를 직접 구현해야 함 | Firestore SDK가 오프라인 캐시·재동기화를 기본 제공 (이 항목만 보면 Firebase가 더 유리) |
| 비용 구조 | 자원 기반 정액제, 예측 가능 | 연산량 기반 과금, 트래픽 증가 시 비용 급증 가능 |

**선택: Supabase.** 오프라인 지원 자체는 Firestore가 기술적으로 더 유리하다는 걸 인정한다. 하지만 이 프로젝트의 평가 포인트가 "오프라인 캐시·네트워크 실패 복구 구조를 직접 설계하고 설명"하는 것이므로, Firestore의 내장 오프라인 기능에 의존하면 오히려 보여줄 아키텍처가 사라진다. 관계형 스키마(FK·cascade)와 RLS가 필요한 성능 리포트 요구사항, 그리고 "구글이 대신 해주는 것 vs 내가 설계한 것"의 차이를 스스로 만들기 위해 Postgres 기반 Supabase + 직접 구현한 Room/WorkManager 동기화를 선택했다.

### 10.2 AI 런타임: LiteRT + MoveNet 직접 통합 vs ML Kit Pose Detection API

| 기준 | ML Kit Pose Detection | LiteRT + MoveNet 직접 통합 |
|---|---|---|
| 랜드마크 개수 | 얼굴·손·발까지 포함한 전신 33개 | 17개 (코·눈·귀·어깨·팔꿈치·손목·엉덩이·무릎·발목) |
| 가속기 제어 | CPU/CPU_GPU "선호도"만 지정 가능, 최종 결정은 ML Kit이 안정성·지연시간을 자체 판단 | Interpreter 생성 시 CPU/GPU delegate를 코드로 직접 명시·강제 가능 |
| 추론 지연 계측 | 내부 블랙박스라 프레임별 정밀 계측이 어려움 | `interpreter.run()` 전후로 타임스탬프 직접 측정 가능 |
| API 안정성 | 베타 상태, SLA·지원중단 정책 대상 아님 → 하위호환성이 깨질 수 있음 | `.tflite` 모델을 프로젝트에 번들 → Google API 변경과 무관하게 동작 |
| 커스터마이징 | 모델 교체·파인튜닝 불가 | 모델 교체·파인튜닝 완전히 가능 |
| 개발 속도 | 코드 몇 줄로 즉시 동작 | 전처리·출력 파싱·스레딩을 직접 구현 |

**선택: LiteRT + MoveNet 직접 통합.** 이 프로젝트는 "콜드스타트·프레임드랍을 측정하고 성능 개선 전후를 증명"하는 게 목표다. ML Kit은 가속기 선택이 "선호도 요청"일 뿐이라 GPU delegate 적용 전/후라는 재현 가능한 A/B 비교 자체가 성립하기 어렵고, 추론 지연을 정밀하게 찍을 훅도 없다. 반면 LiteRT는 delegate를 직접 제어하고 프레임별 지연을 정확히 계측할 수 있어 `performance_metrics` 테이블에 넣을 신뢰성 있는 수치를 만들 수 있다. 또한 33개 중 실제로 쓰는 랜드마크는 하체 5~6개뿐이라, 17개짜리 MoveNet이 오버헤드도 더 적다.

---

## 11. 확장성 설계 (스쿼트 → 다른 맨몸운동·기구운동으로 확장 시)

> 5일 프로젝트 자체는 스쿼트 1종에 스코프를 고정한다. 아래는 "확장 가능성을 고려한 설계"이며, 실제 구현은 인터페이스 시임(seam)만 남기고 각 Analyzer 클래스는 만들지 않는다.

### 11.1 모델 교체 시점 판단 기준

| 확장 단계 | 새로 생기는 문제 | 대응 모델/전략 |
|---|---|---|
| 맨몸운동 추가 (푸시업, 플랭크, 런지, 싯업) | 대부분 한 평면(2D) 관절 각도로 설명 가능 | MoveNet 17kp 그대로 재사용. 운동별 최적 카메라 각도 안내 UI만 추가 |
| 기구운동 추가 (벤치프레스, 데드리프트, 케이블머신) | ① 바벨/기구에 관절이 가려짐(occlusion) ② 바 경로·허리 곡률 등 깊이(depth) 정보가 필요 | MoveNet(2D, 17kp) → MediaPipe BlazePose(GHUM, 3D, 33kp)로 교체 검토. 여전히 온디바이스·delegate 제어 가능 |
| 기구 자체 추적 (바 경로 시각화 등) | 포즈 모델은 사람만 인식, 기구는 못 봄 | 경량 객체탐지 모델(예: EfficientDet-Lite)을 포즈 모델과 나란히 추가 — 여전히 LiteRT 위에서 동작, 런타임 교체가 아니라 모델 추가 |
| 다인 동시 인식 (그룹 클래스) | MoveNet 기본은 단일 포즈 | MoveNet MultiPose 변형 (이 앱 스코프에서는 낮은 우선순위) |

**핵심 원칙**: LiteRT(런타임)는 그대로 유지되고, 바뀌는 건 그 위에 얹는 모델의 개수·종류뿐이다.

### 11.2 아키텍처 확장 포인트

**① Strategy 패턴으로 운동별 분석기 분리**
```kotlin
interface ExercisePoseAnalyzer {
    fun analyze(keypoints: List<Keypoint>): RepEvent?
    fun feedback(keypoints: List<Keypoint>): FormFeedback?
}
// 구현체: SquatAnalyzer, PushupAnalyzer, BenchPressAnalyzer ...
```
카메라→추론 파이프라인은 그대로 두고, "keypoints를 받아 rep/피드백을 뽑아내는" 부분만 운동별로 갈아끼우는 구조. 지금은 `SquatAnalyzer` 하나만 만들지만, 이 인터페이스 자리는 처음부터 남겨둔다.

**② DB 스키마 확장**
현재 `exercise_type text check (in ('squat','pushup'))`는 CHECK 제약이라 확장에 걸림돌이 된다. 운동 종류가 늘어나면:
```sql
create table exercise_types (
  id text primary key,           -- 'squat', 'pushup', 'bench_press' ...
  display_name text not null,
  default_camera_angle text,     -- 'side' | 'front'
  required_keypoints jsonb       -- 이 운동 판정에 필요한 keypoint 목록
);
```
참조 테이블 구조로 바꾸고 `workout_sessions.exercise_type`이 이 테이블을 FK로 참조하게 한다. `rep_records`, `performance_metrics` 스키마는 운동 종류와 무관하게 그대로 재사용 가능 — 처음 설계가 운동 종류에 독립적으로 잘 잡혔다는 뜻.

**③ 운동 선택 방식**
사용자가 시작 전 직접 선택하는 방식을 기본으로 유지한다. keypoint 시퀀스로 운동을 자동 분류하는 건 별도 분류 모델이 필요해 오버엔지니어링 소지가 있으므로 확장 2단계 이후로 미룬다.

**④ 카메라 앵글 안내 UI**
운동마다 최적 카메라 위치가 다르므로(스쿼트=측면, 런지 무릎정렬=정면), 운동 선택 시 안내 화면이 필요해진다.

---

## 12. 회고에 반드시 담을 질문 (미리 기록해두고 5일 내내 업데이트)

- 왜 Room-first 구조를 택했는가 (Supabase 직접 쓰기와 비교해 얻은 것/잃은 것)
- 왜 MoveNet Lightning인가 (Thunder 대비 정확도-속도 트레이드오프, 실측 근거)
- 상태머신 임계값(160°/100°)을 어떻게 정했는가, 실패했던 케이스는 무엇이었는가
- 5일 안에 못 다룬 것: 푸시업, 다인 인식, 야외 강한 역광 등 — 왜 스코프에서 뺐는가
- (신규) DB·AI 런타임을 각각 왜 선택했는가 — 10장의 비교표 기준으로 트레이드오프를 설명할 수 있는가
- (신규) 확장성을 어디까지 미리 설계하고 어디부터 일부러 안 했는가 (오버엔지니어링 경계선)
- (신규) 규칙기반(1단계)만으로 못 잡는 오류를 3단계 분류기가 실제로 얼마나 더 잡아냈는가 — 1·3단계 불일치 로그로 정량 비교했는가
- (신규) 5단계(지속 재학습)를 스코프에서 뺀 게 어떤 한계를 남겼는가 — AI-Hub 데이터에 없는 패턴을 예로 들어 설명할 수 있는가
