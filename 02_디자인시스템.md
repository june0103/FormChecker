# 폼체커 디자인 시스템 — 다크 · 강렬한 네온

> Compose Material3 기반. 코드 구현은 `app/src/main/java/com/mist/formchecker/ui/theme/`에 반영됨 (Color.kt, Type.kt, Theme.kt, Shape.kt, Dimens.kt, Motion.kt).
> v1.1 — 디자인 리뷰(대비율 실측 검증)를 거쳐 컬러 롤 전체 정의, 배너 패턴 수정, Typography 롤 전체 정의, 모션 토큰 추가 반영.

## 왜 이 방향인가

폼체커는 카메라 프리뷰 위에 스켈레톤 오버레이·rep 카운터 HUD·피드백 배너를 얹는 화면(Workout)이 핵심이다. 이 화면은 대비가 약하면 바로 사용성이 떨어지고, 실제 사용 환경(집, 저녁 시간대 홈트)도 밝지 않은 경우가 많다. 그래서 **앱 전체를 다크 단일 테마로 고정**하고(시스템 라이트/다크 설정을 따르지 않음), 포인트 컬러를 라임그린 하나로 제한해 절제된 강렬함을 유지한다.

## 컬러 팔레트

| 토큰 | 값 | 용도 |
|---|---|---|
| `BgBase` | `#0A0A0F` | 최상위 배경 (surfaceDim / surfaceContainerLowest) |
| `SurfaceContainerLow` | `#15151B` | 낮은 elevation 표면 |
| `SurfaceCard` | `#1A1A22` | 카드/시트 표면 (surfaceContainer) |
| `SurfaceContainerHigh` | `#232330` | 높은 elevation 표면 (TopAppBar 등) |
| `SurfaceContainerHighest` | `#2C2C3A` | 최고 elevation 표면 (Dialog 등) |
| `SurfaceBright` | `#35354A` | 가장 밝은 표면 |
| `SurfaceVariant` | `#24242E` | 카드 위 중첩 요소(칩, 인풋) |
| `Outline` / `OutlineVariant` | `#2E2E38` / `#3A3A46` | 구분선, 테두리 |
| `TextPrimary` | `#F5F5F7` | 본문/타이틀 텍스트 |
| `TextMuted` | `#9A9AA5` | 보조 텍스트, 캡션 |
| `TextDisabled` | `#5A5A66` | 비활성 텍스트 |
| `LimeGreen` (Primary) | `#C6FF00` | rep 카운터, 정상 자세, 주요 CTA — **앱 전체에서 유일한 강조색** |
| `LimeGreenDim` | `#8FB800` | Lime의 눌림(pressed) 상태 |
| `LimeGreenContainer` | `#1F2B00` | Lime 톤을 은은하게 쓰는 카드/칩 배경 |
| `FeedbackWarning` / `Container` | `#FF6B35` / `#2E1A10` | 폼 경고 (무릎 정렬, 깊이 부족 등) |
| `FeedbackDanger` / `Container` | `#FF4757` / `#2E0F13` | 하드 에러 (모델 로드 실패, 카메라 불가) |
| `FeedbackInfo` / `Container` | `#4DA3FF` / `#10202E` | 중립 안내 (오프라인 배지, 카메라 각도 조정 안내) |

**규칙**: Lime은 "성공/카운트/CTA" 전용. 그 외 상태는 반드시 Warning/Danger/Info 중 하나로만 표현하고 새 컬러를 추가하지 않는다.

**⚠ 색상만으로 상태를 구분하지 않는다.** 적록색맹(남성 인구 약 8%) 사용자를 고려해, Success(라임)/Warning(오렌지)/Danger(빨강) 배지·배너에는 항상 아이콘(체크/느낌표/X)을 색상과 함께 표시한다. 색만 바꾸고 아이콘 없이 상태를 전달하는 컴포넌트는 만들지 않는다.

**⚠ M3 ColorScheme 롤을 전부 명시적으로 채운다.** 처음 버전에서 background/surface/primary/secondary/error 등 일부만 채웠더니, NavigationBar·TopAppBar·Dialog·Snackbar·BottomSheet가 내부적으로 참조하는 `surfaceContainer*`/`tertiary`/`inverseSurface`/`outlineVariant`/`scrim` 롤에 M3 baseline 기본 팔레트(보라 계열)가 그대로 남아 라임/블랙 테마와 충돌하는 문제가 있었다. `Theme.kt`에서 `darkColorScheme()`의 모든 파라미터를 명시적으로 지정해 해결함.

## 배너/배지 컬러 패턴 (v1.1에서 수정됨)

초기안은 "진한 색 배경 + 흰 글자"였는데, 실측 대비율이 Warning 2.84:1 / Info 2.63:1로 WCAG AA(4.5:1) 기준에 크게 못 미쳐 폐기했다.

- **꽉 찬 배경(솔리드) 배너/버튼**: `FeedbackWarning`/`FeedbackDanger`/`FeedbackInfo` 배경 + **어두운 텍스트**(`OnFeedbackSolid`, `#0A0A0F`). 대비 5.9~7.5:1로 전부 AA 통과.
- **은은한 배경(컨테이너) 배지/칩**: `xxxContainer`(예: `FeedbackWarningContainer #2E1A10`) 배경 + **해당 컬러 텍스트**(예: `FeedbackWarning`). 대비 5.2~5.8:1로 AA 통과. 다크 테마에서 더 자연스러운 "알럿 칩" 패턴이라 실사용은 이쪽을 기본으로 권장.

## 타이포그래피

Material3 Typography의 13개 롤을 전부 Noto Sans KR로 채웠다 (v1.0에서는 7개만 채워, Badge/Tooltip/AssistChip 등이 내부적으로 참조하는 `displayLarge/Medium/Small`, `headlineMedium/Small`, `bodySmall`, `labelMedium`에 M3 기본 폰트가 섞여 나오는 문제가 있었음).

| 스타일 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| `displayLarge/Medium/Small` | 57/45/36sp | Black/Bold/Bold | 초대형 강조 (거의 안 쓰지만 M3 컴포넌트 fallback 대비 정의) |
| `headlineLarge/Medium/Small` | 28/24/22sp | Bold/Bold/SemiBold | 스크린 타이틀, 섹션 타이틀 |
| `titleLarge/Medium/Small` | 20/16/14sp | SemiBold/SemiBold/Medium | 카드 타이틀, 서브 타이틀 |
| `bodyLarge/Medium/Small` | 16/14/12sp | Regular | 본문, 보조 본문, 캡션 |
| `labelLarge/Medium/Small` | 16/13/12sp | SemiBold/Medium/Medium | 버튼, 배지, 캡션 |
| `RepCounterDisplay` | 96sp | Black | Workout 화면 rep 숫자 (2자리 이하) |
| `RepCounterDisplayCompact` | 72sp | Black | rep 숫자가 3자리(100+)일 때 — 좁은 화면 클리핑 방지용으로 추가 |

**폰트: Noto Sans KR** (Google Fonts, OFL 라이선스, 가변 폰트 1개 파일)을 `res/font/noto_sans_kr.ttf`로 로컬 번들. Downloadable Fonts API 대신 파일 자체를 앱에 넣어 "네트워크 없어도 100% 동작" 원칙과 일치시켰다.

- weight별로 `FontVariation.Settings(FontVariation.weight(...))`를 지정해 Regular(400)/Medium(500)/SemiBold(600)/Bold(700)/Black(900) 5단계를 하나의 파일에서 렌더링.
- ⚠ 가변 폰트 weight 축은 API 26+ 전용. `minSdk 24`(API 24~25)에서는 모든 굵기가 Regular로 표시됨(크래시 없음, 5일 스코프에서 감수).

## 스페이싱 · 모서리 · 터치 타겟

- **8pt 그리드**: `Spacing.xs`(4) / `sm`(8) / `md`(16) / `lg`(24) / `xl`(32) / `xxl`(48).
- **모서리 반경**: `Radius.sm`(8) / `md`(12, 버튼) / `lg`(16, 카드) / `pill`(999, 배지).
- **최소 터치 타겟**: `TouchTarget.minSize = 48.dp`. 모든 탭 가능한 요소(버튼, 아이콘 버튼, 칩)는 이 크기 이상 확보 (v1.1에서 추가 — 접근성 가이드 누락 보완).

## 모션 (v1.1에서 추가)

실시간 피드백 앱 특성상 모션이 체감 품질에 크게 기여하는데 v1.0에는 토큰이 전혀 없었다. `Motion.kt`에 정의:

- `durationFast`(120ms): 배지 색 변경, 아이콘 전환
- `durationMedium`(240ms): 피드백 배너 등장/퇴장, 화면 전환
- `durationSlow`(400ms): SessionSummary 진입, 축하 애니메이션
- `repPulseDuration`(180ms): rep 카운트 증가 시 숫자 펄스(scale 1.0→1.15→1.0) 전용
- `standardEasing` / `emphasizedEasing`: 표준 M3 스타일 큐빅 베지어

## 컴포넌트 가이드

- **Primary Button**: `LimeGreen` 배경 + `OnLimeGreen`(검정) 텍스트, `Radius.md`, 최소 `TouchTarget.minSize`. "운동 시작" 등 핵심 CTA에만 사용.
- **Secondary Button**: 투명 배경 + `LimeGreen` 테두리/텍스트.
- **Stat Card** (SessionSummary): `SurfaceCard` 배경, `Radius.lg`, 큰 숫자는 `titleLarge` 이상 + `LimeGreen` 포인트.
- **Feedback Banner** (Workout 화면 하단): `xxxContainer` 배경 + 해당 컬러 텍스트 + 아이콘(체크/느낌표/X 필수), `Motion.durationMedium`로 슬라이드 인/아웃.
- **Sync Badge** (오프라인/동기화 대기): `Radius.pill` 캡슐, `FeedbackInfoContainer` 배경 + `FeedbackInfo` 텍스트 + 아이콘.
- **Skeleton Overlay**: 관절점은 `LimeGreen`, 연결선은 `LimeGreen` 50% 불투명도 — 그라데이션 금지.
- **Rep Counter**: `repCount < 100` → `RepCounterDisplay`(96sp), `repCount >= 100` → `RepCounterDisplayCompact`(72sp). 증가할 때 `Motion.repPulseDuration`으로 펄스.

## 적용 원칙

1. 색상은 항상 `MaterialTheme.colorScheme.*` 또는 `ui/theme/Color.kt`의 토큰을 통해서만 참조한다. 컴포저블 안에 `Color(0xFF...)` 하드코딩 금지.
2. Workout 화면은 이 디자인 시스템에서 예외를 두지 않는다.
3. 팔레트에 색을 추가하고 싶어지면, 먼저 기존 5개 시맨틱 컬러(Success/Warning/Danger/Info/Muted) 중 하나로 표현 가능한지부터 검토한다.
4. 새 컬러 조합을 추가할 때는 반드시 WCAG AA 대비율(일반 텍스트 4.5:1, 큰 텍스트 3:1)을 계산해서 확인한다 — v1.0의 배너 텍스트 대비 실패가 반복되지 않도록.
